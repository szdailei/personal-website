const config = {
  LOADED_TAG: 'article',
  PDFS_DIR: 'pdfs/',
  VIEWPORT: {
    width: 1920,
    height: 1280,
    expectedPdfWidth: 1440,
    expectedPdfHeight: 960,
  },
};

export default config;
